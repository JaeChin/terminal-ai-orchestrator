param(
  [Parameter(ValueFromRemainingArguments = $true)]
  [string[]]$Prompt
)

Write-Host "Running Claude agent..."
Write-Host "Prompt: $Prompt"

# Placeholder until Claude CLI is wired
Write-Host "[Claude] $Prompt"
